{
  "name": "neverlanding",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/browser.mjs",
    "check:catalog": "node tools/check-catalog.mjs",
    "achievements:generate": "python3 tools/generate-achievements.py"
  },
  "devDependencies": { "@playwright/test": "^1.58.2", "wrangler": "^4.0.0" }
}
